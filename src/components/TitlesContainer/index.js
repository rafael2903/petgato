import { Container } from "./styles";

function TitlesContainer( {children} ) {
    return (
        <Container>
            {children}
        </Container>
    );
}

export default TitlesContainer;